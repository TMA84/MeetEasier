/**
 * @file singleRoom.config.js
 * @description Single-room display configuration. Exports the translated label
 * set used by the single-room view (status text, meeting info, booking labels).
 */

import { getSingleRoomDisplayTranslations } from './displayTranslations.js';

export default getSingleRoomDisplayTranslations();
